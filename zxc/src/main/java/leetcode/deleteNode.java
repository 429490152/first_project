package leetcode;

/**
 * 编写一个功能来删除单链表中的节点（尾部除外），只允许访问该节点。
   假设链表是1 -> 2 -> 3 -> 4和你被赋予第三个节点的值3，链表应该1 -> 2 -> 4在调用你的函数后。
 */

/*
    public class deleteNode {
    public void deNode(ListNode node){
        node.val = node.next.val;
        node.next = node.next.next;
    }
    sssss
}
 */

